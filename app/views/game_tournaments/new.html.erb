<h1>New Game tournament</h1>

<%= render 'form', game_tournament: @game_tournament %>

<%= link_to 'Back', game_tournaments_path %> |

<%= link_to 'Add Team', new_team_path(@teams), class: "btn btn-primary" %>
